type Author {
  bookCount: Int!
  born: Int
  id: ID!
  name: String!
}

type Mutation {
  editAuthor(name: String!, setBornTo: Int!): Author
}

type Query {
  authorCount: Int!
  allAuthors: [Author!]!
}
